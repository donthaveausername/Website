import styles from '../../styles/Home.module.css';
import { Container } from 'semantic-ui-react';
import Head from 'next/head';

<Head>
	<title>Commands</title>
</Head>

<Container text>
  <main className={styles['top-padded']}>
    # Commands
    *Note: The bot is not completed yet and will not be available for most of the time or will be broken.*
    
    The bot accepts both slash commands and message commands. The current prefix for message commands is `[DATA EXPUNGED]`.
    ## Utility commands
	  - `def`: define a given science term <br /> 
      Syntax: `def <term>`
    ## WIP
  </main>
</Container>
